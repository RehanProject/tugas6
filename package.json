{
  "name": "my-vue-app",
  "version": "1.0.0",
  "description": "A simple Vue.js project with json-server",
  "main": "index.js",
  "scripts": {
    "serve": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:server": "json-server --watch db.json"
  },
  "dependencies": {
    "vue": "^3.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.2.0",
    "vite": "^2.0.0"
  }
}
